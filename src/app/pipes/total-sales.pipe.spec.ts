import { TotalSalesPipe } from './total-sales.pipe';

describe('TotalSalesPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalSalesPipe();
    expect(pipe).toBeTruthy();
  });
});
