import { InboundSalesPipe } from './inbound-sales.pipe';

describe('InboundSalesPipe', () => {
  it('create an instance', () => {
    const pipe = new InboundSalesPipe();
    expect(pipe).toBeTruthy();
  });
});
